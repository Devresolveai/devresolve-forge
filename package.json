{
  "name": "devresolve-forge",
  "version": "1.0.0",
  "description": "DevResolve — Autonomous engineering ticket deflection for Jira",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "forge deploy",
    "deploy": "forge deploy",
    "deploy:prod": "forge deploy --environment production",
    "tunnel": "forge tunnel",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@forge/api": "^7.1.2",
    "@forge/resolver": "^1.7.0"
  },
  "devDependencies": {
    "@forge/cli": "^12.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
